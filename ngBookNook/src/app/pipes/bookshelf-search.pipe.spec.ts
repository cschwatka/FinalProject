import { BookshelfSearchPipe } from './bookshelf-search.pipe';

describe('BookshelfSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new BookshelfSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
