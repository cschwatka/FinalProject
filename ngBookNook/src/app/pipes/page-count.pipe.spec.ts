import { PageCountPipe } from './page-count.pipe';

describe('PageCountPipe', () => {
  it('create an instance', () => {
    const pipe = new PageCountPipe();
    expect(pipe).toBeTruthy();
  });
});
